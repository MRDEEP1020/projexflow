{
  "buildCommand": "composer install --no-dev && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "public",
  "framework": "laravel",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.php"
    }
  ]
}